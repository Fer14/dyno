# DYNO — mobile app

The FastAPI server and single-file client for DYNO.

Full documentation — rarity, breeding, battles, dataset generation and VAE training — lives in the [project README](../README.md).

```bash
bash run_server.sh   # uvicorn on 0.0.0.0:8000 over HTTPS
```
